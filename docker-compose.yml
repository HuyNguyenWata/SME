version: '3.8'

services:
  sme_backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sme_backend
    restart: unless-stopped
    ports:
      - '3000:3000'
    env_file:
      - .env
