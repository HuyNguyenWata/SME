version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sme-backend
    restart: unless-stopped
    ports:
      - '3000:3000'
    env_file:
      - .env
